const LESSON_PLAN_TARGET = 5;
const LESSON_PLAN_FORM_URL = 'https://forms.cloud.microsoft/r/yJSaXpZkxM';
const WEEKLY_REPORTS_URL = 'https://emiratesschoolsese-my.sharepoint.com/:x:/g/personal/saidah_khwar_moe_sch_ae/IQDFNjvAgJnDSI9bsti8goDnAd3nVvODKMrlq1UE0yapyUw?e=whLCnT';
const TERM_SEQUENCE = ['Term 1','Term 2','Term 3'];
const TERM_MAX_WEEKS = {'Term 1':18,'Term 2':14};
const TERM_STARTS={'Term 1':'2026-08-31','Term 2':'2027-01-04','Term 3':'2027-04-12'};

function uaeToday(){const parts=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Dubai',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date());const o={};parts.forEach(p=>o[p.type]=p.value);return `${o.year}-${o.month}-${o.day}`;}
function dateUTC(s){const [y,m,d]=s.split('-').map(Number);return Date.UTC(y,m-1,d)}
function currentAcademicWeek(){const now=dateUTC(uaeToday()),terms=TERM_SEQUENCE.map(t=>({term:t,start:dateUTC(TERM_STARTS[t])}));let chosen=terms[0];for(const x of terms)if(now>=x.start)chosen=x;let week=Math.floor((now-chosen.start)/(7*86400000))+1;week=Math.max(1,week);const max=TERM_MAX_WEEKS[chosen.term];if(max)week=Math.min(week,max);return {term:chosen.term,week};}
function isCurrentFollowupWeek(){const c=currentAcademicWeek();return state.term===c.term&&Number(state.week)===c.week;}
function openLessonPlanUpload(){if(!isCurrentFollowupWeek()){const c=currentAcademicWeek();return alert(`Uploads are only allowed for the current UAE school week: ${c.term} · Week ${c.week}.`)}window.open(LESSON_PLAN_FORM_URL,'_blank','noopener');}
function openWeeklyReports(){window.open(WEEKLY_REPORTS_URL,'_blank','noopener');}
function openAllLessonPlans(){if(typeof openAllSharedPlans==='function')openAllSharedPlans();else alert('Shared lesson plans are still loading. Please try again in a moment.');}

function normalizedTermWeek(term,week){let termIndex=Math.max(0,TERM_SEQUENCE.indexOf(term));let next=Number.parseInt(week,10);if(!Number.isFinite(next))next=1;while(next<1&&termIndex>0){termIndex-=1;next+=TERM_MAX_WEEKS[TERM_SEQUENCE[termIndex]]||1;}while(TERM_MAX_WEEKS[TERM_SEQUENCE[termIndex]]&&next>TERM_MAX_WEEKS[TERM_SEQUENCE[termIndex]]&&termIndex<TERM_SEQUENCE.length-1){next-=TERM_MAX_WEEKS[TERM_SEQUENCE[termIndex]];termIndex+=1;}const resolvedTerm=TERM_SEQUENCE[termIndex],maximum=TERM_MAX_WEEKS[resolvedTerm];return {term:resolvedTerm,week:Math.max(1,maximum?Math.min(next,maximum):next)};}
function academicWeekInfo(weekNumber){const number=Math.max(1,Number(weekNumber)||1);const termStart=dateUTC(TERM_STARTS[state.term]||TERM_STARTS['Term 1']);const monday=new Date(termStart+(number-1)*7*86400000),friday=new Date(monday.getTime()+4*86400000);const label=new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'short',year:'numeric',timeZone:'UTC'});return {label:`${label.format(monday)} – ${label.format(friday)}`};}
function changeFollowupWeek(delta){const next=normalizedTermWeek(state.term,Number(state.week)+delta);state.term=next.term;state.week=next.week;localStorage.setItem(K,JSON.stringify(state));renderTeachers();buildLessonHeader();}
function renderTeachers(){const canUpload=isCurrentFollowupWeek();teacherGrid.innerHTML=state.teachers.map(teacher=>`<div class="teacher-list-row followup-teacher-row"><div class="name">📁 ${esc(teacher.name)}</div>${canUpload?`<button class="btn primary upload-row" onclick="openLessonPlanUpload()">＋ Upload</button>`:`<span class="view-only-week">View only</span>`}</div>`).join('')||'<p>No teacher folders yet.</p>';if(typeof starTrack!=='undefined'&&starTrack)starTrack.style.display='none';}
function buildLessonHeader(){const grid=document.getElementById('teacherGrid');if(!grid)return;const card=grid.closest('.card');if(!card)return;card.classList.add('lesson-compact','followup-hub');card.querySelector('.lesson-custom-head')?.remove();const weekInfo=academicWeekInfo(state.week),head=document.createElement('div');head.className='lesson-custom-head followup-head';const current=isCurrentFollowupWeek();head.innerHTML=`<div class="lesson-title"><div class="lesson-badge">LP</div><div><h3>Lesson Plan Follow-up Hub</h3><div class="week-line"><span>${esc(state.term)}</span><button class="week-arrow" onclick="changeFollowupWeek(-1)" aria-label="Previous week">‹</button><b>Week ${state.week}</b><button class="week-arrow" onclick="changeFollowupWeek(1)" aria-label="Next week">›</button><span>${weekInfo.label}</span>${current?'<span class="current-week-badge">UAE Current Week</span>':'<span class="view-only-badge">View only · uploads locked</span>'}</div><div class="followup-rules"><span>Target: <b>${LESSON_PLAN_TARGET} lesson plans</b></span><span>Deadline: <b>Monday 12:00 PM</b></span></div></div></div><div class="lesson-head-actions"><button class="btn all-plans-btn" onclick="openAllLessonPlans()">📚 View All Lesson Plans</button><button class="btn pdf-report-btn" onclick="openWeeklyReports()">📊 Live Follow-up Report</button></div>`;card.insertBefore(head,grid);}
(function(){const current=currentAcademicWeek();state.term=current.term;state.week=current.week;localStorage.setItem(K,JSON.stringify(state));const style=document.createElement('style');style.textContent=`.followup-hub .teacher-grid{display:grid!important;grid-template-columns:repeat(2,minmax(0,1fr))!important;gap:8px!important;margin-top:10px!important}.followup-hub .teacher-list-row{display:flex!important;justify-content:space-between!important;align-items:center!important;gap:10px!important;padding:9px 12px!important;min-height:36px!important;border-radius:10px!important}.followup-hub .teacher-list-row .name{font-size:13px!important;white-space:nowrap}.followup-head{display:flex!important;justify-content:space-between!important;align-items:center!important;gap:14px!important;padding-bottom:10px!important;border-bottom:1px solid #e2e7e5!important}.followup-head h3{margin-bottom:3px!important}.week-line{display:flex;align-items:center;gap:7px;color:#74868b;font-size:12px;flex-wrap:wrap}.week-line b{color:#4f646c}.week-arrow{width:25px;height:25px;border:1px solid #d8e1de;border-radius:8px;background:#eef3f1;color:#58736f;font-size:19px;line-height:20px;cursor:pointer;font-weight:800}.week-arrow:hover{background:#dde9e5}.current-week-badge{background:#e2eee8;color:#527465;border:1px solid #cfe1d8;border-radius:999px;padding:3px 7px;font-weight:800}.view-only-badge,.view-only-week{background:#f2eee4;color:#756b57;border:1px solid #e1d7c1;border-radius:999px;padding:3px 7px;font-size:10.5px;font-weight:800}.followup-rules{display:flex;gap:7px;flex-wrap:wrap;margin-top:5px}.followup-rules span{font-size:10.5px;color:#61777c;background:#f1f5f3;border:1px solid #dfe7e4;border-radius:999px;padding:4px 8px}.lesson-head-actions{display:flex;align-items:center;justify-content:flex-end;gap:8px;flex-wrap:wrap}.pdf-report-btn{height:38px;padding:7px 14px!important;white-space:nowrap;background:#789b96!important;color:#fff!important;border-color:#789b96!important;font-weight:800!important}.all-plans-btn{height:38px;padding:7px 14px!important;white-space:nowrap;background:#d9ccb0!important;color:#5f584a!important;border-color:#d0c19e!important;font-weight:800!important}.upload-row{white-space:nowrap;padding:6px 10px!important;font-size:11.5px!important}@media(max-width:850px){.followup-hub .teacher-grid{grid-template-columns:1fr!important}.followup-head{align-items:flex-start!important;flex-direction:column}.lesson-head-actions{justify-content:flex-start}}`;document.head.appendChild(style);uploadPlan=openLessonPlanUpload;renderTeachers();buildLessonHeader();})();