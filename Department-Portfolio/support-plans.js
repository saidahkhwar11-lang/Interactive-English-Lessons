(()=>{
const d=document;
const ALLOWED=/\.(pdf|doc|docx|xls|xlsx)$/i;
const ACCEPT='.pdf,.doc,.docx,.xls,.xlsx,application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document,application/vnd.ms-excel,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet';
function dbOpen(){return new Promise((res,rej)=>{const r=indexedDB.open('EnglishDepartmentPortfolioFiles',1);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains('supportPlans'))r.result.createObjectStore('supportPlans',{keyPath:'id'})};r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}
async function getAll(){const db=await dbOpen();return new Promise((res,rej)=>{const r=db.transaction('supportPlans','readonly').objectStore('supportPlans').getAll();r.onsuccess=()=>res(r.result||[]);r.onerror=()=>rej(r.error)})}
async function put(x){const db=await dbOpen();return new Promise((res,rej)=>{const r=db.transaction('supportPlans','readwrite').objectStore('supportPlans').put(x);r.onsuccess=()=>res();r.onerror=()=>rej(r.error)})}
async function del(id){const db=await dbOpen();return new Promise((res,rej)=>{const r=db.transaction('supportPlans','readwrite').objectStore('supportPlans').delete(id);r.onsuccess=()=>res();r.onerror=()=>rej(r.error)})}
const esc=s=>String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function validFiles(list){return [...(list||[])].filter(f=>ALLOWED.test(f.name))}
async function saveSupportSelection(type,files,label){const fs=validFiles(files);if(!fs.length)return alert('Please choose PDF, Word or Excel files.');await put({id:Date.now()+'-'+Math.random().toString(36).slice(2),type,folder:label||'Uploaded Files',created:Date.now(),files:fs.map(f=>({name:f.name,path:f.webkitRelativePath||f.name,type:f.type,data:f}))});openSupportPlans(type)}
window.openSupportPlans=async function(type){const title=type==='diagnostic'?'Diagnostic Analysis':'CA Analysis';const rows=(await getAll()).filter(x=>x.type===type).sort((a,b)=>b.created-a.created);const body=`<div style="display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:14px"><div><h3 style="margin:0">📁 ${title}</h3><small style="color:#7b878c">Upload PDF, Word, Excel files or a complete folder.</small></div><div class="actions"><label class="btn primary" style="cursor:pointer">+ Upload File<input id="supportFileInput" type="file" accept="${ACCEPT}" multiple hidden></label><label class="btn gold" style="cursor:pointer">+ Upload Folder<input id="supportFolderInput" type="file" webkitdirectory directory multiple hidden></label></div></div><div id="supportRows">${rows.length?rows.map(x=>`<div class="item" style="display:flex;justify-content:space-between;align-items:center;gap:12px;margin:7px 0"><div><b>${esc(x.folder)}</b><br><small>${x.files.length} file${x.files.length===1?'':'s'} · ${new Date(x.created).toLocaleDateString()}</small></div><div class="actions"><button class="btn" onclick="openSupportFolder('${x.id}')">Open</button><button class="btn danger" onclick="deleteSupportFolder('${x.id}','${type}')">Delete</button></div></div>`).join(''):'<div class="notice">No support plans uploaded yet.</div>'}</div>`;
 if(typeof openModal==='function')openModal(body);else alert('Support Plans');
 setTimeout(()=>{
   const fi=d.getElementById('supportFileInput');if(fi)fi.onchange=()=>saveSupportSelection(type,fi.files,'Uploaded Files');
   const fo=d.getElementById('supportFolderInput');if(fo)fo.onchange=()=>{const fs=[...fo.files];const folder=fs.length?((fs[0].webkitRelativePath||fs[0].name).split('/')[0]):'Uploaded Folder';saveSupportSelection(type,fs,folder)};
 },0)
};
window.openSupportFolder=async function(id){const x=(await getAll()).find(v=>v.id===id);if(!x)return;const html=`<h3>📂 ${esc(x.folder)}</h3><p style="color:#7b878c">${x.files.length} uploaded file${x.files.length===1?'':'s'}</p>${x.files.map((f,i)=>`<div class="item" style="display:flex;justify-content:space-between;align-items:center;gap:12px;margin:7px 0"><span>${esc(f.path)}</span><button class="btn" onclick="openSupportFile('${id}',${i})">Open</button></div>`).join('')}`;if(typeof openModal==='function')openModal(html)};
window.openSupportFile=async function(id,i){const x=(await getAll()).find(v=>v.id===id);if(!x||!x.files[i])return;const u=URL.createObjectURL(x.files[i].data);window.open(u,'_blank');setTimeout(()=>URL.revokeObjectURL(u),60000)};
window.deleteSupportFolder=async function(id,type){if(!confirm('Delete this uploaded folder?'))return;await del(id);openSupportPlans(type)};

/* Lesson Plans: PDF / Word / Excel / folder */
window.uploadPlan=function(i){let t=state.teachers[i];openModal(`<h2>${esc(t.name)} · Upload Lesson Plan</h2><p>Current: ${esc(state.term)} · Week ${state.week}</p><label>Grade / class</label><input id="pgrade"><label>Upload file(s)</label><input id="pfile" type="file" accept="${ACCEPT}" multiple><label>Or upload a folder</label><input id="pfolder" type="file" webkitdirectory directory multiple><div class="actions"><button class="btn primary" onclick="savePlanEnhanced(${i})">Add Plan</button><button class="btn" onclick="closeModal()">Cancel</button></div>`)};
window.savePlanEnhanced=function(i){const grade=(d.getElementById('pgrade')?.value||'').trim();const files=validFiles([...(d.getElementById('pfile')?.files||[]),...(d.getElementById('pfolder')?.files||[])]);if(!files.length)return alert('Choose PDF, Word, Excel files or a folder containing them.');const k=weekKey();state.teachers[i].plans[k]=state.teachers[i].plans[k]||[];let remaining=files.length;files.forEach(file=>{const r=new FileReader();r.onload=()=>{state.teachers[i].plans[k].push({grade,file:file.webkitRelativePath||file.name,data:r.result,date:new Date().toISOString()});remaining--;if(!remaining){save();closeModal()}};r.readAsDataURL(file)})};

/* Share & Inspire: PDF / Word / Excel / folder */
window.addShare=function(cat){openModal(`<h2>Add ${esc(cat)}</h2><label>Title</label><input id="stitle"><label>Teacher name</label><input id="steacher"><label>Grade / class</label><input id="sgrade"><label>Short description</label><textarea id="sdesc"></textarea><label>Upload file(s)</label><input id="sfile" type="file" accept="${ACCEPT}" multiple><label>Or upload a folder</label><input id="sfolder" type="file" webkitdirectory directory multiple><div class="actions"><button class="btn primary" onclick="saveShareEnhanced('${String(cat).replace(/'/g,"\\'")}')">Add Evidence</button><button class="btn" onclick="closeModal()">Cancel</button></div>`)};
window.saveShareEnhanced=function(cat){const title=(d.getElementById('stitle')?.value||'').trim(),teacher=(d.getElementById('steacher')?.value||'').trim(),grade=(d.getElementById('sgrade')?.value||'').trim(),desc=(d.getElementById('sdesc')?.value||'').trim();if(!title||!teacher)return alert('Add title and teacher name.');const files=validFiles([...(d.getElementById('sfile')?.files||[]),...(d.getElementById('sfolder')?.files||[])]);if(!files.length){state.shares.push({term:state.term,cat,title,teacher,grade,desc,file:'',data:'',date:new Date().toISOString()});save();closeModal();return}let remaining=files.length;files.forEach(file=>{const r=new FileReader();r.onload=()=>{state.shares.push({term:state.term,cat,title,teacher,grade,desc,file:file.webkitRelativePath||file.name,data:r.result,date:new Date().toISOString()});remaining--;if(!remaining){save();closeModal()}};r.readAsDataURL(file)})};

/* Remove the bottom Important notice highlighted by the user */
setTimeout(()=>{[...d.querySelectorAll('main > .notice')].forEach(n=>{if(/^Important:/i.test((n.textContent||'').trim()))n.remove()})},0);
})();