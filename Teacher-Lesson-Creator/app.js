const $=id=>document.getElementById(id);const slots=new Map();let mainCount=0;const status=$('status'),create=$('createBtn');function toast(s){$('toast').textContent=s;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),1700)}function slotId(c){return c.dataset.slot}function initSource(c){const chk=c.querySelector('.not-from-book'),page=c.querySelector('.activity-page');if(!chk||!page)return;chk.addEventListener('change',()=>{page.disabled=chk.checked;if(chk.checked)page.value='';page.placeholder=chk.checked?'Not from the book':'e.g. 12'})}function initSlot(c){const id=slotId(c);if(!slots.has(id))slots.set(id,[]);initSource(c);const input=c.querySelector('.slot-files'),zone=c.querySelector('.paste-zone');input?.addEventListener('change',()=>addFiles(c,[...input.files]));zone?.addEventListener('paste',e=>{const fs=[...e.clipboardData.files].filter(f=>f.type.startsWith('image/')||f.type.startsWith('audio/')||f.type==='application/pdf');if(fs.length){e.preventDefault();addFiles(c,fs)}});zone?.addEventListener('dragover',e=>{e.preventDefault();zone.classList.add('drag')});zone?.addEventListener('dragleave',()=>zone.classList.remove('drag'));zone?.addEventListener('drop',e=>{e.preventDefault();zone.classList.remove('drag');addFiles(c,[...e.dataTransfer.files])});render(c)}function addFiles(c,files){const id=slotId(c),a=slots.get(id)||[];files.forEach(f=>a.push({id:crypto.randomUUID?.()||Date.now()+Math.random(),file:f,url:f.type.startsWith('image/')?URL.createObjectURL(f):''}));slots.set(id,a);render(c);toast(`${files.length} file${files.length===1?'':'s'} added`)}function render(c){const a=slots.get(slotId(c))||[],box=c.querySelector('.previews');if(!box)return;box.innerHTML='';a.forEach(x=>{const d=document.createElement('div');d.className='preview';d.innerHTML=x.url?`<img src="${x.url}" alt="activity image"><button title="Remove">×</button>`:`<div class="file">${x.file.name}</div><button title="Remove">×</button>`;d.querySelector('button').onclick=()=>{if(x.url)URL.revokeObjectURL(x.url);slots.set(slotId(c),a.filter(y=>y.id!==x.id));render(c)};box.append(d)})}function sourceHtml(){return `<div class="activity-source"><label>📖 Activity Page <input class="activity-page" placeholder="e.g. 14"></label><label class="not-book"><input type="checkbox" class="not-from-book"> Not from the book</label></div>`}function mainCard(n){const d=document.createElement('div');d.className='activity-card';d.dataset.slot='main-'+n;d.innerHTML=`<div class="activity-head"><div><span class="badge main">MAIN ACTIVITY ${n}</span><h3>Main Activity ${n}</h3><p>Optional — paste or upload one or several activity pictures.</p></div><div><span class="ai-chip">✨ AI reads & builds it</span> <button class="remove-main" type="button">Remove</button></div></div>${sourceHtml()}<div class="paste-zone" tabindex="0"><b>📋 Paste activity picture here</b><span>Ctrl + V • drag & drop • or</span><label class="browse">Browse Files<input class="slot-files" type="file" accept="image/*,.pdf,audio/*" multiple></label></div><div class="previews"></div>`;d.querySelector('.remove-main').onclick=()=>{(slots.get(d.dataset.slot)||[]).forEach(x=>x.url&&URL.revokeObjectURL(x.url));slots.delete(d.dataset.slot);d.remove();renumber()};return d}function renumber(){const cs=[...$('mainActivities').children];const oldData=cs.map(c=>[c.dataset.slot,slots.get(c.dataset.slot)||[]]);oldData.forEach(([id])=>slots.delete(id));cs.forEach((c,i)=>{const id='main-'+(i+1);c.dataset.slot=id;slots.set(id,oldData[i][1]);c.querySelector('.badge').textContent='MAIN ACTIVITY '+(i+1);c.querySelector('h3').textContent='Main Activity '+(i+1)});mainCount=cs.length;$('addMain').disabled=mainCount>=5}function addMain(){if(mainCount>=5)return;const c=mainCard(mainCount+1);$('mainActivities').append(c);mainCount++;initSlot(c);$('addMain').disabled=mainCount>=5}document.querySelectorAll('.activity-card[data-slot]').forEach(initSlot);$('addMain').onclick=addMain;addMain();addMain();function filesFor(id){return(slots.get(id)||[]).map(x=>x.file)}function activityMeta(c){const notBook=c.querySelector('.not-from-book')?.checked||false;return{page:notBook?'':(c.querySelector('.activity-page')?.value.trim()||''),not_from_book:notBook}}async function createLesson(){const grade=$('grade').value,term=$('term').value,skill=$('skill').value,starterCard=document.querySelector('[data-slot="starter"]'),exitCard=document.querySelector('[data-slot="exit"]'),starter=filesFor('starter'),mains=[...$('mainActivities').children].map(c=>({card:c,slot:c.dataset.slot,files:filesFor(c.dataset.slot)})).filter(x=>x.files.length),exit=filesFor('exit');if(!grade||!skill){status.textContent='Please choose the grade and skill.';status.className='status error';return}if(!starter.length&&!mains.length&&!exit.length){status.textContent='Paste or upload at least one lesson activity picture.';status.className='status error';return}const endpoint=window.LESSON_CREATOR_API||'';if(!endpoint){status.textContent='Design ready ✓ — secure AI connection is still required before automatic generation can run.';status.className='status error';toast('Creator design is ready');return}create.disabled=true;create.textContent='✨ AI IS BUILDING YOUR LESSON…';status.textContent='Reading your textbook activities and preparing the complete lesson…';status.className='status';try{const form=new FormData();form.append('grade',grade);form.append('term',term);form.append('skill',skill);form.append('lesson_title',$('lessonTitle').value.trim());form.append('book_pages',$('bookPages').value.trim());form.append('prompt',$('lessonPrompt').value.trim());form.append('starter_meta',JSON.stringify(activityMeta(starterCard)));starter.forEach(f=>form.append('starter',f));mains.forEach((m,i)=>{form.append(`main_${i+1}_meta`,JSON.stringify(activityMeta(m.card)));m.files.forEach(f=>form.append(`main_${i+1}`,f))});form.append('exit_ticket_meta',JSON.stringify(activityMeta(exitCard)));exit.forEach(f=>form.append('exit_ticket',f));form.append('include_charter','true');const r=await fetch(endpoint,{method:'POST',body:form});if(!r.ok)throw new Error(await r.text()||`Request failed (${r.status})`);const data=await r.json();$('afterPanel').hidden=false;status.textContent='Lesson created successfully.';status.className='status ready';const bs=[...$('afterPanel').querySelectorAll('button')],ls=[data.lesson_url,data.plan_url,data.edit_url,data.library_url,data.backup_url];bs.forEach((b,i)=>{if(ls[i]){b.disabled=false;b.onclick=()=>window.open(ls[i],i===3?'_self':'_blank')}});$('afterPanel').scrollIntoView({behavior:'smooth'})}catch(e){status.textContent='Could not create the lesson: '+e.message;status.className='status error'}finally{create.disabled=false;create.textContent='✨ CREATE MY LESSON'}}create.onclick=createLesson;